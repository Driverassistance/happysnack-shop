cd /c/work/telegram_shop

# Создаем новый render.yaml
cat > render.yaml << 'EOF'
services:
  # Backend API + Bot (вместе!)
  - type: web
    name: happysnack-app
    env: python
    region: frankfurt
    plan: free
    rootDir: backend
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python start_all.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: DATABASE_URL
        fromDatabase:
          name: happysnack-db
          property: connectionString
      - key: BOT_TOKEN
        sync: false
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: ADMIN_TELEGRAM_ID
        value: "473294026"
      - key: PORT
        value: "10000"

databases:
  - name: happysnack-db
    databaseName: happysnack
    user: happysnack
    region: frankfurt
    plan: free
EOF